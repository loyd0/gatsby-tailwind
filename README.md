# Gatsby and Tailwind CSS Starter

`yarn` to get started 

Make sure you have the gatsby cli installed 

# Features

* Tailwind
* Font-Awesome
* DotEnv (supports .env loading)
* Alias imports (allows you to do "@components" instead of "../src/components" etc)